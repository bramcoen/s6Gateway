apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springgateway
spec:
  rules:
  - host: s6.coenders.party
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: springgateway
            port:
              number: 8080